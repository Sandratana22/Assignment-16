#ifndef GRADE_HPP
#define GRADE_HPP

namespace average {
    double average(int grade[], int size);
}

namespace max {
    int max(int grade[], int size); 
}

namespace min {
    int min(int grade[], int size); 
}

namespace aboveAverage {
    void aboveAverage(int grade[], int size, double average); 
}

namespace compareGrade {
    void compareGrade(int grade[], int size);
}

#endif
